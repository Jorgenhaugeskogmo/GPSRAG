# Railway ultra-minimal requirements - absolute minimum for basic functionality
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.8.2
python-multipart==0.0.6
python-dotenv==1.0.0
aiofiles==23.1.0

# Only if OpenAI is absolutely needed
# openai==1.26.0

# Only if PDF processing is absolutely needed  
# pypdf==4.2.0

# Build timestamp: 2025-06-10-09-15 